// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosformsdemo.domain.formidregistration.hibernate;

import java.util.*;
import java.time.OffsetDateTime;
import org.test.aptosformsdemo.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.test.aptosformsdemo.domain.formidregistration.*;
import org.test.aptosformsdemo.specialization.*;
import org.test.aptosformsdemo.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateFormIdRegistrationStateRepository implements FormIdRegistrationStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("FormSequenceId", "FormId", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public FormIdRegistrationState get(Long id, boolean nullAllowed) {
        FormIdRegistrationState.SqlFormIdRegistrationState state = (FormIdRegistrationState.SqlFormIdRegistrationState)getCurrentSession().get(AbstractFormIdRegistrationState.SimpleFormIdRegistrationState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractFormIdRegistrationState.SimpleFormIdRegistrationState();
            state.setFormSequenceId(id);
        }
        return state;
    }

    @Transactional
    public void save(FormIdRegistrationState state) {
        FormIdRegistrationState s = state;
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(FormIdRegistrationState detached) {
        FormIdRegistrationState persistent = getCurrentSession().get(AbstractFormIdRegistrationState.SimpleFormIdRegistrationState.class, detached.getFormSequenceId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(FormIdRegistrationState persistent, FormIdRegistrationState detached) {
        ((AbstractFormIdRegistrationState) persistent).merge(detached);
    }

}

