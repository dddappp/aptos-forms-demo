// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosformsdemo.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.time.OffsetDateTime;
import org.test.aptosformsdemo.domain.*;
import org.test.aptosformsdemo.specialization.*;
import org.test.aptosformsdemo.domain.formidregistration.*;
import static org.test.aptosformsdemo.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "FormIdRegistrations", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class FormIdRegistrationResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private FormIdRegistrationApplicationService formIdRegistrationApplicationService;


    /**
     * Retrieve.
     * Retrieve FormIdRegistrations
     */
    @GetMapping
    @Transactional(readOnly = true)
    public FormIdRegistrationStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<FormIdRegistrationState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FormIdRegistrationResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FormIdRegistrationMetadata.aliasMap.containsKey(n) ? FormIdRegistrationMetadata.aliasMap.get(n) : n));
            states = formIdRegistrationApplicationService.get(
                c,
                FormIdRegistrationResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            FormIdRegistrationStateDto.DtoConverter dtoConverter = new FormIdRegistrationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFormIdRegistrationStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve FormIdRegistrations in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<FormIdRegistrationStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<FormIdRegistrationState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> FormIdRegistrationResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (FormIdRegistrationMetadata.aliasMap.containsKey(n) ? FormIdRegistrationMetadata.aliasMap.get(n) : n));
            states = formIdRegistrationApplicationService.get(
                c,
                FormIdRegistrationResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = formIdRegistrationApplicationService.getCount(c);

            FormIdRegistrationStateDto.DtoConverter dtoConverter = new FormIdRegistrationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<FormIdRegistrationStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toFormIdRegistrationStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves FormIdRegistration with the specified ID.
     */
    @GetMapping("{formSequenceId}")
    @Transactional(readOnly = true)
    public FormIdRegistrationStateDto get(@PathVariable("formSequenceId") Long formSequenceId, @RequestParam(value = "fields", required = false) String fields) {
        try {
            Long idObj = formSequenceId;
            FormIdRegistrationState state = formIdRegistrationApplicationService.get(idObj);
            if (state == null) { return null; }

            FormIdRegistrationStateDto.DtoConverter dtoConverter = new FormIdRegistrationStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toFormIdRegistrationStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (FormIdRegistrationMetadata.aliasMap.containsKey(n) ? FormIdRegistrationMetadata.aliasMap.get(n) : n));
            count = formIdRegistrationApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create.
     * Create FormIdRegistration
     */
    @PostMapping @ResponseStatus(HttpStatus.CREATED)
    public Long post(@RequestBody CreateOrMergePatchFormIdRegistrationDto.CreateFormIdRegistrationDto value,  HttpServletResponse response) {
        try {
            FormIdRegistrationCommand.CreateFormIdRegistration cmd = value;//.toCreateFormIdRegistration();
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            Long idObj = formIdRegistrationApplicationService.createWithoutId(cmd);

            return idObj;
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    /**
     * Create or update.
     * Create or update FormIdRegistration
     */
    @PutMapping("{formSequenceId}")
    public void put(@PathVariable("formSequenceId") Long formSequenceId, @RequestBody CreateOrMergePatchFormIdRegistrationDto value) {
        try {
            if (value.getOffChainVersion() != null) {
                value.setCommandType(Command.COMMAND_TYPE_MERGE_PATCH);
                FormIdRegistrationCommand.MergePatchFormIdRegistration cmd = (FormIdRegistrationCommand.MergePatchFormIdRegistration) value.toSubclass();
                FormIdRegistrationResourceUtils.setNullIdOrThrowOnInconsistentIds(formSequenceId, cmd);
                cmd.setRequesterId(SecurityContextUtil.getRequesterId());
                formIdRegistrationApplicationService.when(cmd);
                return;
            }
            throw DomainError.named("unsupportedOperation", "Unsupported HTTP PUT to create, aggregate Id %1$s", formSequenceId);
        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            FormIdRegistrationMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return FormIdRegistrationResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

 
    public static class FormIdRegistrationResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(Long formSequenceId, org.test.aptosformsdemo.domain.formidregistration.FormIdRegistrationCommand value) {
            Long idObj = formSequenceId;
            if (value.getFormSequenceId() == null) {
                value.setFormSequenceId(idObj);
            } else if (!value.getFormSequenceId().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", formSequenceId, value.getFormSequenceId());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, FormIdRegistrationMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, FormIdRegistrationMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (FormIdRegistrationMetadata.aliasMap.containsKey(fieldName)) {
                return FormIdRegistrationMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (FormIdRegistrationMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = FormIdRegistrationMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static FormIdRegistrationStateDto[] toFormIdRegistrationStateDtoArray(Iterable<Long> ids) {
            List<FormIdRegistrationStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                FormIdRegistrationStateDto dto = new FormIdRegistrationStateDto();
                dto.setFormSequenceId(i);
                states.add(dto);
            });
            return states.toArray(new FormIdRegistrationStateDto[0]);
        }

    }

}

