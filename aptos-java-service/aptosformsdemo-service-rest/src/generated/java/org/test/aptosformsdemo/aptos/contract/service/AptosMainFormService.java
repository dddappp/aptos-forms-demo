// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosformsdemo.aptos.contract.service;

import com.github.wubuku.aptos.utils.NodeApiClient;
import org.test.aptosformsdemo.domain.*;
import org.test.aptosformsdemo.domain.mainform.*;
import org.test.aptosformsdemo.aptos.contract.repository.*;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.stream.*;
import java.util.*;
import java.math.*;

@Service
public class AptosMainFormService {

    @Autowired
    private MainFormStateRepository mainFormStateRepository;


    private AptosMainFormStateRetriever aptosMainFormStateRetriever;

    @Autowired
    public AptosMainFormService(
        @Value("${aptos.contract.address}")
        String aptosContractAddress,
        NodeApiClient aptosNodeApiClient,
        AptosAccountRepository aptosAccountRepository
    ) {
        this.aptosMainFormStateRetriever = new AptosMainFormStateRetriever(
                aptosNodeApiClient,
                aptosContractAddress,
                aptosAccountRepository,
                signerAddress -> {
                    MainFormState.MutableMainFormState s = new AbstractMainFormState.SimpleMainFormState();
                    s.setSignerAddress(signerAddress);
                    return s;
                }
        );
    }

    @Transactional
    public void updateMainFormState(String signerAddress) {
        MainFormState mainFormState = aptosMainFormStateRetriever.retrieveMainFormState(signerAddress);
        if (mainFormState == null) {
            return;
        }
        mainFormStateRepository.merge(mainFormState);
    }

}

