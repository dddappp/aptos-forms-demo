// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosformsdemo.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import org.test.aptosformsdemo.domain.*;
import java.util.Date;
import java.math.BigInteger;
import org.test.aptosformsdemo.specialization.*;
import org.test.aptosformsdemo.domain.aptosformsdemoglobal.*;
import static org.test.aptosformsdemo.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "AptosFormsDemoGlobals", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class AptosFormsDemoGlobalResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private AptosFormsDemoGlobalApplicationService aptosFormsDemoGlobalApplicationService;


    /**
     * Retrieve.
     * Retrieve AptosFormsDemoGlobals
     */
    @GetMapping
    @Transactional(readOnly = true)
    public AptosFormsDemoGlobalStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<AptosFormsDemoGlobalState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> AptosFormsDemoGlobalResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (AptosFormsDemoGlobalMetadata.aliasMap.containsKey(n) ? AptosFormsDemoGlobalMetadata.aliasMap.get(n) : n));
            states = aptosFormsDemoGlobalApplicationService.get(
                c,
                AptosFormsDemoGlobalResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            AptosFormsDemoGlobalStateDto.DtoConverter dtoConverter = new AptosFormsDemoGlobalStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAptosFormsDemoGlobalStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve AptosFormsDemoGlobals in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<AptosFormsDemoGlobalStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<AptosFormsDemoGlobalState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> AptosFormsDemoGlobalResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (AptosFormsDemoGlobalMetadata.aliasMap.containsKey(n) ? AptosFormsDemoGlobalMetadata.aliasMap.get(n) : n));
            states = aptosFormsDemoGlobalApplicationService.get(
                c,
                AptosFormsDemoGlobalResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = aptosFormsDemoGlobalApplicationService.getCount(c);

            AptosFormsDemoGlobalStateDto.DtoConverter dtoConverter = new AptosFormsDemoGlobalStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<AptosFormsDemoGlobalStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toAptosFormsDemoGlobalStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves AptosFormsDemoGlobal with the specified ID.
     */
    @GetMapping("{formAndAccountAddress}")
    @Transactional(readOnly = true)
    public AptosFormsDemoGlobalStateDto get(@PathVariable("formAndAccountAddress") String formAndAccountAddress, @RequestParam(value = "fields", required = false) String fields) {
        try {
            FormAndAddress idObj = AptosFormsDemoGlobalResourceUtils.parseIdString(formAndAccountAddress);
            AptosFormsDemoGlobalState state = aptosFormsDemoGlobalApplicationService.get(idObj);
            if (state == null) { return null; }

            AptosFormsDemoGlobalStateDto.DtoConverter dtoConverter = new AptosFormsDemoGlobalStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAptosFormsDemoGlobalStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (AptosFormsDemoGlobalMetadata.aliasMap.containsKey(n) ? AptosFormsDemoGlobalMetadata.aliasMap.get(n) : n));
            count = aptosFormsDemoGlobalApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{formAndAccountAddress}/_commands/AdminWithdrawPayment_123_Vault")
    public void adminWithdrawPayment_123_Vault(@PathVariable("formAndAccountAddress") String formAndAccountAddress, @RequestBody AptosFormsDemoGlobalCommands.AdminWithdrawPayment_123_Vault content) {
        try {

            AptosFormsDemoGlobalCommands.AdminWithdrawPayment_123_Vault cmd = content;//.toAdminWithdrawPayment_123_Vault();
            FormAndAddress idObj = AptosFormsDemoGlobalResourceUtils.parseIdString(formAndAccountAddress);
            if (cmd.getFormAndAccountAddress() == null) {
                cmd.setFormAndAccountAddress(idObj);
            } else if (!cmd.getFormAndAccountAddress().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", formAndAccountAddress, cmd.getFormAndAccountAddress());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            aptosFormsDemoGlobalApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            AptosFormsDemoGlobalMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{formAndAccountAddress}/_events/{version}")
    @Transactional(readOnly = true)
    public AptosFormsDemoGlobalEvent getEvent(@PathVariable("formAndAccountAddress") String formAndAccountAddress, @PathVariable("version") long version) {
        try {

            FormAndAddress idObj = AptosFormsDemoGlobalResourceUtils.parseIdString(formAndAccountAddress);
            //AptosFormsDemoGlobalStateEventDtoConverter dtoConverter = getAptosFormsDemoGlobalStateEventDtoConverter();
            return aptosFormsDemoGlobalApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{formAndAccountAddress}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public AptosFormsDemoGlobalStateDto getHistoryState(@PathVariable("formAndAccountAddress") String formAndAccountAddress, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            FormAndAddress idObj = AptosFormsDemoGlobalResourceUtils.parseIdString(formAndAccountAddress);
            AptosFormsDemoGlobalStateDto.DtoConverter dtoConverter = new AptosFormsDemoGlobalStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toAptosFormsDemoGlobalStateDto(aptosFormsDemoGlobalApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  AptosFormsDemoGlobalStateEventDtoConverter getAptosFormsDemoGlobalStateEventDtoConverter() {
    //    return new AptosFormsDemoGlobalStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return AptosFormsDemoGlobalResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class AptosFormsDemoGlobalResourceUtils {

        public static FormAndAddress parseIdString(String idString) {
            TextFormatter<FormAndAddress> formatter = AptosFormsDemoGlobalMetadata.URL_ID_TEXT_FORMATTER;
            return formatter.parse(idString);
        }

        public static void setNullIdOrThrowOnInconsistentIds(String formAndAccountAddress, AptosFormsDemoGlobalCommand value) {
            FormAndAddress idObj = parseIdString(formAndAccountAddress);
            if (value.getFormAndAccountAddress() == null) {
                value.setFormAndAccountAddress(idObj);
            } else if (!value.getFormAndAccountAddress().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", formAndAccountAddress, value.getFormAndAccountAddress());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, AptosFormsDemoGlobalMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, AptosFormsDemoGlobalMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (AptosFormsDemoGlobalMetadata.aliasMap.containsKey(fieldName)) {
                return AptosFormsDemoGlobalMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (AptosFormsDemoGlobalMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = AptosFormsDemoGlobalMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static AptosFormsDemoGlobalStateDto[] toAptosFormsDemoGlobalStateDtoArray(Iterable<FormAndAddress> ids) {
            List<AptosFormsDemoGlobalStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                AptosFormsDemoGlobalStateDto dto = new AptosFormsDemoGlobalStateDto();
                dto.setFormAndAccountAddress(i);
                states.add(dto);
            });
            return states.toArray(new AptosFormsDemoGlobalStateDto[0]);
        }

    }

}

