// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.aptosformsdemo.domain.aptosformsdemomainform;

import java.util.*;
import java.util.function.Consumer;
import org.dddml.support.criterion.Criterion;
import java.math.BigInteger;
import org.test.aptosformsdemo.domain.*;
import java.time.OffsetDateTime;
import org.test.aptosformsdemo.specialization.*;

public abstract class AbstractAptosFormsDemoMainFormApplicationService implements AptosFormsDemoMainFormApplicationService {

    private EventStore eventStore;

    protected EventStore getEventStore()
    {
        return eventStore;
    }

    private AptosFormsDemoMainFormStateRepository stateRepository;

    protected AptosFormsDemoMainFormStateRepository getStateRepository() {
        return stateRepository;
    }

    private AptosFormsDemoMainFormStateQueryRepository stateQueryRepository;

    protected AptosFormsDemoMainFormStateQueryRepository getStateQueryRepository() {
        return stateQueryRepository;
    }

    private AggregateEventListener<AptosFormsDemoMainFormAggregate, AptosFormsDemoMainFormState> aggregateEventListener;

    public AggregateEventListener<AptosFormsDemoMainFormAggregate, AptosFormsDemoMainFormState> getAggregateEventListener() {
        return aggregateEventListener;
    }

    public void setAggregateEventListener(AggregateEventListener<AptosFormsDemoMainFormAggregate, AptosFormsDemoMainFormState> eventListener) {
        this.aggregateEventListener = eventListener;
    }

    public AbstractAptosFormsDemoMainFormApplicationService(EventStore eventStore, AptosFormsDemoMainFormStateRepository stateRepository, AptosFormsDemoMainFormStateQueryRepository stateQueryRepository) {
        this.eventStore = eventStore;
        this.stateRepository = stateRepository;
        this.stateQueryRepository = stateQueryRepository;
    }

    public void when(AptosFormsDemoMainFormCommands.Submit c) {
        update(c, ar -> ar.submit(c.getFr_5pqi(), c.getFr_duif(), c.getFr_6i34(), c.getFr_8xjs(), c.getFr_9c3f(), c.getFr_4ok6(), c.getFr_b3ub(), c.getFr_1z7o(), c.getFr_d8rw(), c.getFr_dy3l(), c.getFr_6f68(), c.getFr_47yy(), c.getFr_gh3o(), c.getFr_fbba(), c.getFr_hhzp(), c.getSingle_text1(), c.getOffChainVersion(), c.getCommandId(), c.getRequesterId(), c));
    }

    public AptosFormsDemoMainFormState get(String id) {
        AptosFormsDemoMainFormState state = getStateRepository().get(id, true);
        return state;
    }

    public Iterable<AptosFormsDemoMainFormState> getAll(Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getAll(firstResult, maxResults);
    }

    public Iterable<AptosFormsDemoMainFormState> get(Iterable<Map.Entry<String, Object>> filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<AptosFormsDemoMainFormState> get(Criterion filter, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().get(filter, orders, firstResult, maxResults);
    }

    public Iterable<AptosFormsDemoMainFormState> getByProperty(String propertyName, Object propertyValue, List<String> orders, Integer firstResult, Integer maxResults) {
        return getStateQueryRepository().getByProperty(propertyName, propertyValue, orders, firstResult, maxResults);
    }

    public long getCount(Iterable<Map.Entry<String, Object>> filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public long getCount(Criterion filter) {
        return getStateQueryRepository().getCount(filter);
    }

    public AptosFormsDemoMainFormEvent getEvent(String signerAddress, long version) {
        AptosFormsDemoMainFormEvent e = (AptosFormsDemoMainFormEvent)getEventStore().getEvent(toEventStoreAggregateId(signerAddress), version);
        if (e != null) {
            ((AptosFormsDemoMainFormEvent.SqlAptosFormsDemoMainFormEvent)e).setEventReadOnly(true); 
        } else if (version == -1) {
            return getEvent(signerAddress, 0);
        }
        return e;
    }

    public AptosFormsDemoMainFormState getHistoryState(String signerAddress, long version) {
        EventStream eventStream = getEventStore().loadEventStream(AbstractAptosFormsDemoMainFormEvent.class, toEventStoreAggregateId(signerAddress), version - 1);
        return new AbstractAptosFormsDemoMainFormState.SimpleAptosFormsDemoMainFormState(eventStream.getEvents());
    }


    public AptosFormsDemoMainFormAggregate getAptosFormsDemoMainFormAggregate(AptosFormsDemoMainFormState state) {
        return new AbstractAptosFormsDemoMainFormAggregate.SimpleAptosFormsDemoMainFormAggregate(state);
    }

    public EventStoreAggregateId toEventStoreAggregateId(String aggregateId) {
        return new EventStoreAggregateId.SimpleEventStoreAggregateId(aggregateId);
    }

    protected void update(AptosFormsDemoMainFormCommand c, Consumer<AptosFormsDemoMainFormAggregate> action) {
        String aggregateId = c.getSignerAddress();
        EventStoreAggregateId eventStoreAggregateId = toEventStoreAggregateId(aggregateId);
        AptosFormsDemoMainFormState state = getStateRepository().get(aggregateId, false);
        boolean duplicate = isDuplicateCommand(c, eventStoreAggregateId, state);
        if (duplicate) { return; }

        AptosFormsDemoMainFormAggregate aggregate = getAptosFormsDemoMainFormAggregate(state);
        aggregate.throwOnInvalidStateTransition(c);
        action.accept(aggregate);
        persist(eventStoreAggregateId, c.getOffChainVersion() == null ? AptosFormsDemoMainFormState.VERSION_NULL : c.getOffChainVersion(), aggregate, state); // State version may be null!

    }

    private void persist(EventStoreAggregateId eventStoreAggregateId, long version, AptosFormsDemoMainFormAggregate aggregate, AptosFormsDemoMainFormState state) {
        getEventStore().appendEvents(eventStoreAggregateId, version, 
            aggregate.getChanges(), (events) -> { 
                getStateRepository().save(state); 
            });
        if (aggregateEventListener != null) {
            aggregateEventListener.eventAppended(new AggregateEvent<>(aggregate, state, aggregate.getChanges()));
        }
    }

    protected boolean isDuplicateCommand(AptosFormsDemoMainFormCommand command, EventStoreAggregateId eventStoreAggregateId, AptosFormsDemoMainFormState state) {
        boolean duplicate = false;
        if (command.getOffChainVersion() == null) { command.setOffChainVersion(AptosFormsDemoMainFormState.VERSION_NULL); }
        if (state.getOffChainVersion() != null && state.getOffChainVersion() > command.getOffChainVersion()) {
            Event lastEvent = getEventStore().getEvent(AbstractAptosFormsDemoMainFormEvent.class, eventStoreAggregateId, command.getOffChainVersion());
            if (lastEvent != null && lastEvent instanceof AbstractEvent
               && command.getCommandId() != null && command.getCommandId().equals(((AbstractEvent) lastEvent).getCommandId())) {
                duplicate = true;
            }
        }
        return duplicate;
    }

    public static class SimpleAptosFormsDemoMainFormApplicationService extends AbstractAptosFormsDemoMainFormApplicationService {
        public SimpleAptosFormsDemoMainFormApplicationService(EventStore eventStore, AptosFormsDemoMainFormStateRepository stateRepository, AptosFormsDemoMainFormStateQueryRepository stateQueryRepository)
        {
            super(eventStore, stateRepository, stateQueryRepository);
        }
    }

}

